s="muted">${esc(d.document_type)} ・ ${fmt(d.expires_on)||'期限未登録'}</div></div>${badge(d._text,d._kind)}</div>`).join('')||'<div class="card empty">現在、期限対応はありません。</div>'}</div>`}
function rosterPage(){setTitle('作業員情報','WORKER ROSTER');return `<div class="card"><div class="section-head"><div><h2>作業員名簿データ</h2><p>第5号等の帳票へ転記する正本情報。帳票出力は次工程で固定します。</p></div></div>${state.partners.length?`<div class="table-wrap"><table><thead><tr><th>氏名</th><th>ふりがな</th><th>職種</th><th>健康保険</th><th>厚生年金</th><th>雇用保険</th><th>CCUS技能者ID</th></tr></thead><tbody>${state.partners.map(p=>{const w=state.workers.find(x=>x.partner_id===p.id)||{};return `<tr><td class="name-cell">${esc(p.full_name)}</td><td>${esc(w.furigana||'未登録')}</td><td>${esc(w.occupation||'未登録')}</td><td>${esc(w.health_insurance||'未登録')}</td><td>${esc(w.pension||'未登録')}</td><td>${esc(w.employment_insurance||'未登録')}</td><td>${esc(w.ccus_skill_worker_id||'')}</td></tr>`}).join('')}</tbody></table></div>`:'<div class="empty">登録者がいません。</div>'}</div>`}
function auditPage(){setTitle('監査','INTERNAL AUDIT');const issues=[];state.partners.forEach(p=>{if(p.invoice_status==='要確認')issues.push(`${p.display_name}: インボイス状態が要確認`);if(p.invoice_status==='あり'&&!p.invoice_number)issues.push(`${p.display_name}: インボイス番号なし`);if(!p.phone)issues.push(`${p.display_name}: 電話番号なし`)});state.documents.forEach(d=>{if(d.read_status!=='確定')issues.push(`${partner(d.partner_id)?.display_name||''}: ${d.document_type} が${d.read_status}`)});return `<div class="hero"><div class="hero-card"><div class="eyebrow">AUDIT RESULT</div><h2>${issues.length?'要確認 '+issues.length+'件':'監査異常なし'}</h2><p>推測で埋めず、未確認情報を明示します。</p></div><div class="hero-card"><div class="eyebrow">SECURITY</div><h2>RLS 有効</h2><p>本人ログインのデータだけ読書き可能。証明書Storageも非公開です。</p></div></div><div class="section-head"><div><h2>要確認</h2></div></div><div class="alert-list">${issues.map(x=>`<div class="alert-row info"><div>${esc(x)}</div>${badge('要確認','warn')}</div>`).join('')||'<div class="card empty">現在、要確認項目はありません。</div>'}</div>`}

function render(){syncPill();let html='';if(state.page==='home')html=home();else if(state.page==='attendance')html=attendancePage();else if(state.page==='partners')html=state.selected?.startsWith?.('new')?partnerForm():partnersPage();else if(state.page==='partner-edit')html=partnerForm(partner(state.selected));else if(state.page==='profile'){profilePage(state.selected).then(h=>{$('#content').innerHTML=h;bind();hydratePhotos()});return}else if(state.page==='alerts')html=alertsPage();else if(state.page==='roster')html=rosterPage();else html=auditPage();$('#content').innerHTML=html;bind();hydratePhotos()}

function formObj(f){return Object.fromEntries(new FormData(f).entries())}
function displayName(d){if(d.registration_type==='法人')return d.company_name?.trim()||d.full_name;if(d.registration_type==='個人事業主・一人親方')return d.trade_name?.trim()||d.full_name;return d.full_name}
async function savePartner(f){if(!state.session)return toast('先にログインしてください');const d=formObj(f);d.contact_line=f.elements.contact_line.value==='1';d.contact_email=f.elements.contact_email.value==='1';d.base_daily_rate=Number(d.base_daily_rate);d.display_name=displayName(d);['company_name','trade_name','representative_name','address','invoice_number','birth_date','bank_name','branch_name','account_type','account_number','account_holder','notes'].forEach(k=>{if(d[k]==='')d[k]=null});const id=d.id;delete d.id;if(d.invoice_status==='あり'&&!d.invoice_number)return toast('インボイス番号を入力してください');let res;if(id){const before=partner(id);res=await sb.from('partners').update(d).eq('id',id).select().single();if(!res.error)await log('partner',id,'更新',`${d.display_name} 登録情報更新`,before,res.data)}else{res=await sb.from('partners').insert(d).select().single();if(!res.error)await log('partner',res.data.id,'新規登録',`${d.display_name} 新規登録`,null,res.data)}if(res.error)return toast(res.error.message);toast(id?'更新しました':'登録しました');state.selected=null;state.page='partners';await loadAll()}
async function saveAttendance(f){if(!state.session)return toast('先にログインしてください');const d=formObj(f),p=partner(d.partner_id);if(!p)return toast('人物を選択してください');const row={partner_id:d.partner_id,work_date:d.work_date,site_name:d.site_name.trim(),work_description:d.work_description||null,man_days:Number(d.man_days),source_base_daily_rate:p.base_daily_rate,source_invoice_status:p.invoice_status,applied_daily_rate:Number(d.applied_daily_rate),expense:Number(d.expense||0),notes:d.notes||null,source:'PWA'};const res=await sb.from('attendance').insert(row).select().single();if(res.error)return toast(res.error.message);await log('attendance',res.data.id,'追加',`${p.display_name} ${row.work_date} ${row.site_name}`,null,res.data);toast('出面を登録しました');await loadAll()}
async function uploadFile(file,folder){if(!file)return null;const ext=(file.name.split('.').pop()||'bin').toLowerCase();const uid=state.session.user.id;const path=`${uid}/${folder}/${crypto.randomUUID()}.${ext}`;const r=await sb.storage.from(C.storageBucket).upload(path,file,{upsert:false,contentType:file.type});if(r.error)throw r.error;return path}
async function saveDoc(f){const d=formObj(f),file=f.elements.file.files[0];let path=null;try{if(file)path=await uploadFile(file,`documents/${state.selected}`)}catch(e){return toast('ファイル保存失敗: '+e.message)}const row={partner_id:state.selected,document_type:d.document_type,expires_on:d.expires_on||null,read_status:d.read_status,storage_path:path,mime_type:file?.type||null,original_filename:file?.name||null,expiry_required:true};const r=await sb.from('documents').insert(row).select().single();if(r.error)return toast(r.error.message);await log('document',r.data.id,'追加',`${partner(state.selected)?.display_name} ${d.document_type}`,null,r.data);toast('証明書を追加しました');await loadAll()}
async function uploadPhoto(){const file=$('#photoFile')?.files[0];if(!file)return toast('写真を選択してください');let path;try{path=await uploadFile(file,`photos/${state.selected}`)}catch(e){return toast('写真保存失敗: '+e.message)}const row={partner_id:state.selected,storage_path:path,mime_type:file.type,original_filename:file.name};const r=await sb.from('profile_photos').upsert(row,{onConflict:'partner_id'}).select().single();if(r.error)return toast(r.error.message);await log('photo',r.data.id,'更新','顔写真を登録',null,r.data);toast('顔写真を登録しました');await loadAll()}

function bind(){
  $('#quickAttend')?.addEventListener('click',()=>go('attendance'));$('#quickPartner')?.addEventListener('click',()=>{state.page='partners';state.selected='new';renderNav();render()});$('#addPartner')?.addEventListener('click',()=>{state.selected='new';render()});$('#cancelPartner')?.addEventListener('click',()=>go('partners'));$('#editPartner')?.addEventListener('click',()=>{state.page='partner-edit';renderNav();render()});
  $('#personGrid')?.querySelectorAll('.person-card').forEach(x=>x.onclick=()=>go('profile',x.dataset.id));
  $('#partnerForm')?.addEventListener('submit',e=>{e.preventDefault();savePartner(e.currentTarget)});$('#attendanceForm')?.addEventLi